/**
 * Marcador de posição para desenvolvimento local / cópia para o Hub.
 * Substitua pelo ficheiro real do Hub se já existir (renovação de sessão, avisos, etc.).
 */
(function () {
  if (typeof window === "undefined") return;
  window.__sessionReminderStub = true;
})();
